<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:minWidth="360dp"
    android:orientation="vertical"
    android:padding="24dp"
    android:background="?attr/colorSurfaceContainerHigh">

    <TextView
        android:id="@+id/dialog_exit_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/quiz_exit_confirm_title"
        android:textAppearance="@style/AppTextSection"
        android:textColor="?attr/colorOnSurface" />

    <TextView
        android:id="@+id/dialog_exit_message"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="@string/quiz_exit_confirm_message"
        android:textAppearance="@style/AppTextBody"
        android:textColor="?attr/colorOnSurfaceVariant" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginTop="20dp">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/dialog_exit_cancel"
            style="@style/AppButtonSecondary"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginEnd="8dp"
            android:minHeight="48dp"
            android:text="@string/quiz_exit_confirm_negative"
            app:cornerRadius="12dp" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/dialog_exit_confirm"
            style="@style/AppButtonPrimary"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:minHeight="48dp"
            android:text="@string/quiz_exit_confirm_positive"
            app:cornerRadius="12dp" />
    </LinearLayout>
</LinearLayout>
